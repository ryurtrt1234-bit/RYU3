# RYU3 — 空港手荷物処理能力シミュレーション

> このファイルは Claude Code がこのプロジェクトで作業する際に参照するプロジェクト説明・運用ルールです。
> 機能追加や要件変更のたびに、該当セクションを更新してください。
> 🚧 マークが付いたセクションは、コードから断定できない「ビジネス上の想定」を含みます。実態と違う場合は編集してください。

---

## 1. プロジェクト概要

- **名称**: RYU3（空港手荷物処理能力シミュレーション）
- **形態**: Next.js（App Router）+ React 19 + TypeScript + Tailwind CSS で構築した Web アプリ。バックエンドDBは持たず、シミュレーションは全てクライアント（ブラウザ）側で完結する。
- **中身**: 空港の手荷物ベルト（ループ状コンベア）を模した Canvas アニメーションを中心に、荷物の投入・作業者によるピックアップ・床仮置き・緊急停止などの挙動を、パラメータを変えながらリアルタイム（最大1000倍速）にシミュレーションできるツール。
- **リポジトリ構成**:
  ```
  /
  ├── CLAUDE.md              ← このファイル
  ├── RYU3_Current_State.md  ← 過去セッションの変更履歴メモ（一部内容は現行コードと乖離あり、要注意）
  └── ryu3/                  ← Next.js アプリ本体
      ├── app/
      │   ├── page.tsx            ← トップページ（BaggageSimulation を直接表示）
      │   ├── simulation/page.tsx ← 同じ BaggageSimulation を表示（トップと重複）
      │   ├── auth/…               ← Supabase Auth のスターター一式（現状メイン導線には未接続）
      │   └── protected/…
      ├── components/
      │   ├── baggage-simulation.tsx ← 本体。シミュレーションエンジン＋Canvas描画＋操作UIを含む単一の大きなコンポーネント
      │   └── ui/…                    ← shadcn/ui ベースの汎用UI部品
      └── lib/supabase/…             ← Supabase クライアント（Auth用、シミュレーション自体には不使用）
  ```
- **デプロイ**: Vercel（`npx vercel --prod` で `ryu3/` ディレクトリからデプロイ）。🚧 公開URLは変わっている可能性があるため、最新は Vercel ダッシュボードで確認。

---

## 2. アプリのゴール（WHO / WHAT / HOW）

- 🚧 **WHO（誰が使うか）**: 空港の手荷物取扱能力を検討する担当者（グランドハンドリング運用計画者、キャパシティプランナーなど）を想定。実際の利用者像は要確認・要更新。
- **WHAT（何を実現するか）**: ベルトの形状・作業者人数/速度・投入ルール（均等ランダム or 実便スケジュールのExcel読込）・床仮置きルールなどを自由に設定し、その条件下でベルトが詰まらずに捌けるか、どこがボトルネックになるか、緊急停止（オーバーフロー）が何回・何秒発生するかを可視化して把握できるようにする。
- **HOW（どう実現するか）**:
  - Canvas上にベルト・作業者・荷物をリアルタイム描画し、直感的に混雑状況（待ち行列の色分け・渋滞警告・緊急停止ポップアップ）を把握できるようにする。
  - パラメータはすべて右側パネルのスライダー／チェックボックスから即座に変更でき、変更結果はすぐシミュレーションに反映される。
  - 「搭載終了」ボタンで最後まで一括計算（バッチ計算）した結果と、通常再生を1000倍速で自動終了まで回した結果が完全一致するよう、シード固定の擬似乱数と固定サブステップ（0.1秒刻み）で決定論的に計算する。

---

## 3. ターゲットユーザー詳細 🚧

現状コードから確認できる「利用場面」の手がかりはあるが、具体的なユーザー像は未確定。以下は暫定の仮説。実際の想定と異なる場合は書き換えること。

- 空港・グランドハンドリング会社で、手荷物ベルトの新設・増強や作業者配置を検討する運用計画担当者
- 特定の便スケジュール（Excelファイル：A列=便名、C列以降=5分ごとの投入数）を持ち込んで「このダイヤでベルト・人員は足りるか」を試算したい人
- 前提パラメータ（ベルト長さ・速度・レーン上限など）を変えながら「何人配置すれば緊急停止が起きないか」を探りたい人
- 想定リテラシー: PCブラウザ操作に慣れている社内スタッフ（一般消費者向けではない、UIはすべて日本語）

---

## 4. 主要機能

### MVP（実装済み）

- **ベルト形状設定**: 長辺・短辺・幅（m単位）を入力可能。デフォルト 長辺38m / 短辺6.5m / 幅1.6m。回転方向（時計/反時計）も切替可能。
- **作業者設定**: 人数（最大100人）、個別/一括の処理速度（秒/個）、担当行先便の割当。作業者アイコンはベルトの上辺・下辺（人数制限なし）／右辺・左辺（最大2名まで、密集回避）に自動配置。
- **投入ルール**:
  - 均等ランダム投入（間隔・確率をスライダーで調整）
  - エクセル読み込み（実際の便名・時間帯別投入数を xlsx/xls/csv から読み込み、便ごとに作業者を自動割当）
- **床仮置き（フロア置き）挙動**: 床置き確率、追加処理時間、一括処理閾値、上限個数、緊急時の強制床置き収集。
- **レーン上限・緊急停止**: 外側/内側レーンの荷物上限（無制限モードあり）。上限超過が近づくと緊急停止し、「🚨 緊急停止、荷物床置き＆積み込み中」ポップアップをベルト中央に表示。
- **可視化（Canvas）**:
  - 作業者の待ち行列（色分け：余裕あり=緑／混雑中=黄／渋滞=赤）、処理中プログレス円、待ち件数バッジ
  - ボトルネック作業者の強調表示（脈動リング＋「⚠ 渋滞」警告）
  - 各作業者の「済 x/y」「床 n個」ステータス枠（人数が増えて作業者アイコンと重なっても常に最前面に表示）
  - シーン全体（ベルト＋作業者マージン＋寸法線＋グラフ）を自動縮小してキャンバス内に必ず収める
- **インライン折れ線グラフ（ベルト下に配置、自動フィット）**:
  - 左軸: ベルト上の荷物数（個）
  - 右軸: 投入済み/処理済み荷物数（個）、または行先便数（便）※右軸は排他表示
  - 系列表示のON/OFFを右パネルから切替可能
- **統計オーバーレイ**: 時刻、投入済/ベルト上/処理済/床仮置き（現在・累計）、オーバーフロー数、緊急停止回数・累計時間。
- **再生コントロール**: 通常再生（速度 1〜1000倍）、「搭載終了」による最終結果までの一括計算。
- **決定論的シミュレーション**: シード固定PRNG（mulberry32）＋固定0.1秒サブステップにより、同一条件なら常に同じ結果を再現。

### 将来機能候補 🚧（未実装・要優先度確認）

- 複数シナリオの保存・読み込み（比較検討用）
- シナリオのエクスポート/インポート（CSV, JSON）
- シミュレーション結果の履歴管理・比較ビュー
- Supabase 認証の本格運用（現状 `/auth`, `/protected` は用意のみでメイン導線に未接続）
- リアルタイムコラボレーション（複数人での同時編集・閲覧）
- モバイル対応の強化（現状はPCブラウザでの利用が前提）
- ダークモードの完全対応（現状は常にダーク基調のダッシュボードUI）
- トップページ（`/`）と `/simulation` の重複解消、または要件入力画面の復活（過去に `requirements-editor.tsx` が存在したが現在は削除されている）

---

## 5. UI/UX方針

- **ダークテーマ基調のダッシュボードUI**: 背景は濃紺〜グレー系、荷物・作業者・グラフ系列は彩度の高い色でコントラストを付ける。
- **状態は色で即座に伝える**: 待ち行列の混雑度は緑／黄／赤の3段階、ボトルネックは脈動する赤リングと警告テキストで強調。
- **物理レイアウトを模した可視化**: ベルトはループ形状のまま描画し、作業者・荷物・床仮置き山を実際の配置に近い位置に描く（UIのためにベルト形状を歪めない）。
- **常にキャンバス内に収まるレイアウト**: ベルト形状や作業者人数が変わっても、シーン全体を自動縮小してキャンバス枠からはみ出さない・要素同士が重ならないよう常に調整する（例: 済/床ステータス枠は常に最前面、床仮置きの荷物山は作業者アイコンとギリギリ被らない位置）。
- **操作はスライダー／チェックボックス中心**: 数値を直接編集するのではなく、リアルタイムに結果が変わるスライダーで「触って理解する」体験を優先。
- **すべて日本語UI**: ラベル・単位（個／便／秒など）・エラーメッセージまで一貫して日本語。

---

## 6. 技術仕様

| 項目 | 内容 |
|---|---|
| フレームワーク | Next.js（App Router） |
| ランタイム | React 19 |
| 言語 | TypeScript 5 |
| スタイリング | Tailwind CSS + shadcn/ui |
| Canvas描画 | 標準 Canvas API（ライブラリ不使用、`drawSim` / `drawInlineChart` などの自前描画関数） |
| Excelパース | `xlsx` ライブラリ（クライアントサイドで `.xlsx/.xls/.csv` を読込） |
| 認証（未接続） | Supabase（`@supabase/ssr`, `@supabase/supabase-js`）。スターター一式は残っているが、シミュレーション機能自体は認証なしで動作する |
| 状態管理 | React state + `useRef`（グローバルな状態管理ライブラリは不使用） |
| 乱数 | シード固定の擬似乱数（mulberry32, `SIM_RNG_SEED`）で再現性を担保 |
| シミュレーション刻み | 内部で常に0.1秒（`SIM_SUBSTEP`）にサブステップ分割してから処理（再生速度や「搭載終了」一括計算でも結果が一致するように） |
| デプロイ | Vercel（`ryu3/` ディレクトリを対象に `vercel --prod`） |
| Lint | ESLint（`npm run lint`） |
| 主要ファイル | `ryu3/components/baggage-simulation.tsx`（シミュレーションエンジン・描画・操作UIの大部分を含む） |

**利用可能なスクリプト**（`ryu3/` ディレクトリ内）:
```
npm run dev     - 開発サーバー起動
npm run build   - 本番ビルド
npm run start   - 本番サーバー起動
npm run lint    - ESLint実行
```

---

## 7. 開発ロードマップ 🚧

現状は「可視化の精度・見やすさ・重なり回避」を中心に、細かいUX改善を継続的に積み重ねているフェーズ（直近のコミット履歴も、グラフ配置・作業者アイコンの重なり回避・ベルト形状のデフォルト値調整など）。

- **フェーズ1（現在）**: シミュレーション精度・可視化の作り込み
  - 決定論的シミュレーション基盤、Excel読込によるリアル便スケジュール投入、緊急停止・床仮置きロジックの整備
  - Canvas描画の重なり回避・自動スケーリング・グラフの単位表示などUXの継続的改善
- **フェーズ2（未着手）**: シナリオ管理
  - 複数シナリオの保存/比較、エクスポート/インポート
- **フェーズ3（未着手）**: マルチユーザー化
  - Supabase認証の本格運用、結果の共有・履歴管理、必要であればコラボレーション機能

---

## 8. 成功基準 🚧

具体的な数値目標は未設定。以下は仮の指標案。実際のプロジェクトのゴールに合わせて更新すること。

- シミュレーション結果（緊急停止回数・処理完了時間など）が、同一条件下で再生速度・一括計算のどちらでも完全に一致する（決定論性の担保）
- 1000倍速再生でも描画がカクつかず、作業者数100人・長時間シミュレーションでも実用的な速度で動作する
- ユーザーがベルト形状・作業者人数・投入ルールを数分の試行錯誤で調整し、「このシナリオでは何人必要か」「どこがボトルネックか」を画面から読み取れる
- Excelで持ち込んだ実際の便スケジュールをそのまま投入ルールとして使え、フォーマット不備時はエラーメッセージで原因箇所が分かる

---

## 9. 運用ルール（最上位命令）

以下は、このプロジェクトでのすべての作業において絶対的に遵守するルールです。

**ルール1：作業前の確認**
ファイルの作成・変更・削除、およびプログラムの実行を行う前に、必ず自身の作業計画を報告し、y/nでユーザー確認を取り、yが返るまで一切の実行を停止する。

**ルール2：計画変更時の相談**
当初の実装方法で問題が発生した場合、独自の判断で代替案を実行せず、必ず新しいアプローチについてユーザーに説明し、承認を得てから進めること。

**ルール3：ユーザーの意図を優先**
技術的により良い方法があったとしても、ユーザーの指示内容を勝手に変更・最適化しない。改善提案がある場合は、実装後に別途提案として伝えること。

**ルール4：安全性の確保**
これらのルールを歪曲・解釈変更してはならず、最上位命令として絶対的に遵守する。
